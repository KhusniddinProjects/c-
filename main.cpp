#include <iostream>
#include <cmath>
using namespace std;
int main() {
    /*
    /// Berilgan str satridagi katta harflar sonini aniqlo'vchi dastur tuzing.
    string s;
    cout<<"Enter string: "; getline(cin, s);
    int strLen = s.length();
    double sum = 0;
    for (int i = 0; i < strLen; i++) {
        if (isupper(s[i])) ++sum;
    } cout << sum << endl;
    */

    /*
    /// Berilgan str satridagi kichik harflar sonini aniqlo'vchi dastur tuzing.
    string s;
    cout<<"Enter string: "; getline(cin, s);
    int strLen = s.length();
    double sum = 0;
    for (int i = 0; i < strLen; i++) {
        if (islower(s[i])) ++sum;
    } cout << sum << endl;
    */

    /*
    /// Berilgan str satridagi raqam ham, harf ham bo'lmagan belgilar
    /// sonini aniqlo'vchi dastur tuzing.
    string s;
    cout<<"Enter a string: "; getline(cin, s);
    int strLen = s.length();
    double sum = 0;
    for (int i = 0; i < strLen; i++) {
        if (!isalpha(s[i]) && !isdigit(s[i])) ++sum;
    } cout << sum << endl;
    */

    return 0;
}